extends layout.pug

prepend styles
    link(rel="stylesheet", href="/styles/addContent.css")

prepend scripts
    //-script(src="/scripts/FILENAME.js")


block content
    //-CONTENT-STARTS-HERE-//
    
    section.img
        //- img.box-shadow(src='/assets/Portrait_Placeholder.png', alt='')
        label(for='.img') Some files may be unsupported
        form(method='post', enctype='multipart/form-data')
            input.select(type='file', name='upload')
            input(type='submit', name='uploadfile', value='Upload selected file')
        form(action="/editSlideshow")
            input(type="submit" value="Embed slide Video").divider
        form(action="/editSlideshow")
            input(type="submit" value="Return to Editor").divider

    section#message
        h2 
        

